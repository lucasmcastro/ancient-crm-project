<h1>Listing issue_statuses</h1>

<table>
  <tr>
    <th>Creator</th>
    <th>Name</th>
  </tr>

<% @issue_statuses.each do |issue_status| %>
  <tr>
    <td><%=h issue_status.creator %></td>
    <td><%=h issue_status.name %></td>
    <td><%= link_to 'Show', issue_status %></td>
    <td><%= link_to 'Edit', edit_issue_status_path(issue_status) %></td>
    <td><%= link_to 'Destroy', issue_status, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New issue_status', new_issue_status_path %>